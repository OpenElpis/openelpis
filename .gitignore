# OS / editor cruft
.DS_Store
*.swp

# Python build artifacts
__pycache__/
*.py[cod]

# Local DB backups / dumps
*.dump

# Agent / working files
.claude/

# Markdown is private by default (runbooks, design notes, strategy, outreach).
# Only the public README and the architecture blueprint are committed — any NEW .md
# is ignored automatically; add a "!" exception below if it should be public.
*.md
!README.md
!architecture.md

# Operational scripts & infra (server IPs, Oracle OCIDs, emails, deploy) — keep private
deploy/
server2/setup-db.sh
server2/provision-a1-db.sh
server2/app/deploy-app.sh
server2/app/openelpis-app.service
server2/app/openelpis-tunnel.service

# Internal strategy / outreach — keep private
proposal/

# Personal founder & name-SEO pages — generated for the live site, not relevant to the
# open-source repo (GitHub visitors don't need them).
site/founder/
site/cofounder/
site/ali-tajbakhsh/
site/alan-tajbakhsh/
site/mohammadali-tajbakhsh/
site/tr/ali-tajbakhsh/
site/tr/alan-tajbakhsh/
site/tr/mohammadali-tajbakhsh/
